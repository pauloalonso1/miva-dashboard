import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Serve arquivos estáticos de /public automaticamente
  // O dashboard legado fica em /index.html
};

export default nextConfig;
